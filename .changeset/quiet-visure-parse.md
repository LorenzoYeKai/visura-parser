---
'visura-parser': minor
---

Add a deterministic text-based PDF parser with the INDA Visura output schema,
typed input errors, ISO date handling, positioned field extraction, and a
privacy-safe reference corpus verifier.
