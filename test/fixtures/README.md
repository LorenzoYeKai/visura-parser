# Test fixtures

Only synthetic or fully anonymized documents belong in this directory.

Put real local documents in `private/`. Git ignores that directory. Never copy
text from a private document into a snapshot or test failure without checking
it for personal data first.
